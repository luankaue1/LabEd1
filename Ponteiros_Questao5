#include <stdio.h>

int maior(int *a, int *b) {
    if (*a > *b) {
        return *a;
    } else {
        return *b;
    }
}
int main() {
    int num1 = 15, num2 = 7;
    int max = maior(&num1, &num2);
    printf("O maior valor e: %d\n", max);
    return 0;
}