#include <stdio.h>

int main() {
    int x = 10;
    int *p;
    p = &x;
    printf("Valor de x: %d\n", x);
    printf("Endereco de memoria de x: %p\n", &x);
    printf("Valor armazenado em p (endereco de x): %p\n", p);
    printf("Valor apontado por p: %d\n", *p);
    return 0;
}