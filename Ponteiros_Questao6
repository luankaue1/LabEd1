#include <stdio.h>

int main() {
    int v[5] = {1, 2, 3, 4, 5};
    int *p = v;
    printf("Elementos do vetor:\n");
    for (int i = 0; i < 5; i++) {
        printf("%d ", *(p + i));
    }
    printf("\n");
    return 0;
}