#include <stdio.h>

void soma(int *a, int *b, int *resultado) {
    *resultado = *a + *b;
}
int main() {
    int num1 = 10, num2 = 20, resultado;
    soma(&num1, &num2, &resultado);
    printf("A soma e: %d\n", resultado);
    return 0;
}